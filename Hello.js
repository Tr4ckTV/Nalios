function Hello() {
  const hello = String.fromCharCode(72, 101, 108, 108, 111);

  const virgule = String.fromCharCode(44);

  const space = String.fromCharCode(32);

  const name = String.fromCharCode(78, 97, 108, 105, 111, 115);

  const exclam = String.fromCharCode(33);

  console.log(hello + virgule + space + name + exclam);
}

Hello();
